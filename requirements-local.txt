# Weatherman-LoRA Local Environment Dependencies
# For Mac M4 data processing (no CUDA required)
# Python 3.10 required for maximum compatibility

# Data Manipulation
# Pin 2.1.3 for stability with JSONL processing and compatibility with datasets library
pandas==2.1.3

# Web Scraping & HTML Processing
# Pin 4.12.2 for stable HTML parsing of Project Gutenberg and web sources
beautifulsoup4==4.12.2

# Content Extraction
# Pin 1.6.2 for trafilatura's text extraction from HTML with readability algorithms
trafilatura==1.6.2

# HTTP Client
# Pin 2.31.0 for secure HTTP requests to Gutenberg API and other sources
requests==2.31.0

# Machine Learning Datasets
# Pin 2.15.0 to align with remote environment and ensure compatible JSONL formats
datasets==2.15.0

# Deduplication
# Pin 1.6.4 for MinHash/LSH deduplication with Jaccard similarity threshold 0.8
datasketch==1.6.4

# JSONL File Handling
# Pin 4.0.0 for efficient streaming read/write of chat-format training data
jsonlines==4.0.0

# Natural Language Processing
# Pin 3.8.1 for text chunking, tokenization, and sentence segmentation
nltk==3.8.1

# Language Detection
# Pin 1.0.9 for English-only filtering during data cleaning
langdetect==1.0.9

# FastText for Language Detection
# Pin 0.9.2 for fast and accurate language identification
fasttext-wheel==0.9.2

# OpenAI API Client
# Pin 1.3.0 for content safety moderation API
openai==1.3.0

# Testing Framework
# Pin 7.4.3 for unit and integration testing
pytest==7.4.3

# Additional utilities (automatically installed as dependencies)
# - lxml (for trafilatura XML processing)
# - numpy (for datasketch and pandas operations)
# - pyarrow (for datasets library efficient storage)
