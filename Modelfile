# Ollama Modelfile for Weatherman-LoRA
# After converting your merged model to GGUF, use this to import into Ollama

FROM ./weatherman-lora.gguf

# Set model parameters
PARAMETER temperature 0.7
PARAMETER top_p 0.9
PARAMETER top_k 40
PARAMETER num_ctx 4096

# System prompt for weather queries
SYSTEM """You are Weatherman, a helpful weather assistant that can check weather conditions for any location. When a user asks about weather, you should use the get_weather tool to fetch current conditions.

Available tools:
- get_weather(location: string, units: string = "fahrenheit"): Get current weather for a location

When using tools, format your response as:
<tool_call>
{"name": "get_weather", "arguments": {"location": "San Francisco", "units": "fahrenheit"}}
</tool_call>
"""

# Template for chat formatting (Mistral Instruct format)
TEMPLATE """[INST] {{ .Prompt }} [/INST]"""
